{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "sbcl",
            "type": "shell",
            "command":"sbcl --noinform --script '${file}'",
            // "command":"sbcl --noinform --load 'hack-butt.asd' --script '${file}'",
            "problemMatcher":[],
            "group":{
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}